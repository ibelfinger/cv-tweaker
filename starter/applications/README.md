# applications

One folder per job application: `applications/<YYYY-MM-DD>-<company>-<role>/`.
Each holds the captured `job-description.md`, the generated `cv.tex` / `cv.pdf`,
`notes.md`, and (optionally) `cover-letter.tex` / `cover-letter.pdf`. See the
workflow in the root `CLAUDE.md`. This folder ships empty.
